# Configuration for the MCP Gateway
PORT=10000
CONFIG_PATH=/Users/gregory/g/projects/llm/roma/rfk_jr/rfkjr.yaml

# Server Mode (uncomment one)
# FASTMCP_DEV=1    # Run in development mode with MCP Inspector
# FASTMCP_HTTP=1   # Run as HTTP server
# (If neither is set, the server will run in stdio mode)
